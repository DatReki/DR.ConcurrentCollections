# DR.ConcurrentCollections
`DR.ConcurrentCollections` is a library which contains thread safe collections the currently implemented collections are:
* ConcurrentObservableCollection

### ConcurrentObservableCollection
`ConcurrentObservableCollection` is a thread safe version of the `ObservableCollection`. It can be used the same way the regular `ObservableCollection` is utilized.